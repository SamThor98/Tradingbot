# Kronos inference microservice dependencies.
# torch is installed separately in the Dockerfile (CPU wheel index) to keep the
# image small; for local dev `pip install torch` pulls the default wheel.
torch>=2.2,<3
transformers>=4.40
huggingface_hub>=0.23
safetensors>=0.4
einops>=0.7
numpy>=1.26
pandas>=2.0
tqdm>=4.66
fastapi>=0.110
uvicorn[standard]>=0.29
